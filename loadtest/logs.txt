This file previously contained sample k6 output from the old polling-based `/consume` benchmark.

The project now uses SSE delivery via `GET /stream`, and the current `loadtest/e2e.js` script measures publish-side HTTP behavior only.
